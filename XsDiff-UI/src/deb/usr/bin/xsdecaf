#!/usr/bin/env bash
set -euo pipefail

JAR="/opt/xsdecaf/xsdecaf-ui.jar"

if ! command -v java >/dev/null 2>&1; then
  echo "Error: Java runtime not found. Please install OpenJDK 11+." >&2
  exit 1
fi

exec java -Dsun.awt.X11.XWMClass=xsdecaf -jar "$JAR" "$@"

